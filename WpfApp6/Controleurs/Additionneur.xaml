<UserControl x:Class="Projet.Controleurs.Additionneur"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Projet.Controleurs"
            >
    <Grid Name="adder" Height="80" Width="80" Background="Transparent" MouseDown="Grid_MouseDown" MouseMove="Grid_MouseMove" MouseUp="Grid_MouseUp">
        <Grid.RenderTransform>
            <TranslateTransform x:Name="tt"/>
        </Grid.RenderTransform>
        <Grid.RowDefinitions>
            <RowDefinition Height="5"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="5"/>
        </Grid.RowDefinitions>
        <Rectangle x:Name="rectangle" Stroke="Black" StrokeThickness="2"  Grid.Row="1"  />
        <TextBox  x:Name="le_name" Grid.Row="1" Grid.Column="1"  VerticalAlignment="Center"  HorizontalAlignment="Center" Height="20"  Foreground="Black" FontFamily="Roboto" FontSize="12" />

    </Grid>
</UserControl>
